<h2>Search Results</h2>

<% @search_results.each do |search_result| %>
	<p>
		<%= search_result.gene_name %>
		<%= search_result.variant %>
		<%= search_result.PMID %>
		<%= search_result.primary_study_p_value %>
	</p>
<% end %>