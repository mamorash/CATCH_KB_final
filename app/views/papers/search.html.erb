<h2>Search Results</h2>

<% @search_results.each do |search_result| %>
	<p>
		<%= search_result.PMID %>
		<%= search_result.study_date %>
		<%= search_result.candidate_or_GWAS %>
		<%= search_result.num_SNPs %>
	</p>
<% end %>